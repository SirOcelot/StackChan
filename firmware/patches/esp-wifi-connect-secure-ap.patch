diff --git a/include/wifi_configuration_ap.h b/include/wifi_configuration_ap.h
--- a/include/wifi_configuration_ap.h
+++ b/include/wifi_configuration_ap.h
@@ -30,6 +30,8 @@ public:
     void SetSsidPrefix(const std::string &ssid_prefix);
     void SetLanguage(const std::string &&language);
     void SetLanguage(const std::string &language);
+    void SetPassword(const std::string &&password);
+    void SetPassword(const std::string &password);
     void Start();
     void Stop();
 #if !CONFIG_IDF_TARGET_ESP32P4
@@ -59,6 +61,7 @@ private:
     EventGroupHandle_t event_group_;
     std::string ssid_prefix_;
     std::string language_;
+    std::string password_;
     esp_event_handler_instance_t instance_any_id_;
     esp_event_handler_instance_t instance_got_ip_;
     esp_timer_handle_t scan_timer_ = nullptr;
diff --git a/include/wifi_manager.h b/include/wifi_manager.h
--- a/include/wifi_manager.h
+++ b/include/wifi_manager.h
@@ -47,6 +47,7 @@ enum class WifiEvent {
 struct WifiManagerConfig {
     std::string ssid_prefix = "ESP32";    // AP mode SSID prefix
     std::string language = "zh-CN";       // Web UI language
+    std::string ap_password;               // Empty preserves the stock open AP
     
     // Station mode scan interval with exponential backoff
     int station_scan_min_interval_seconds = 10;   // Initial scan interval (fast retry)
@@ -84,6 +85,7 @@ public:
     bool IsConfigMode() const;
     std::string GetApSsid() const;
     std::string GetApWebUrl() const;
+    std::string GetApPassword() const;
 
     // ==================== Power ====================
     
diff --git a/wifi_configuration_ap.cc b/wifi_configuration_ap.cc
--- a/wifi_configuration_ap.cc
+++ b/wifi_configuration_ap.cc
@@ -61,6 +61,16 @@ void WifiConfigurationAp::SetLanguage(const std::string &language)
     language_ = language;
 }
 
+void WifiConfigurationAp::SetPassword(const std::string &&password)
+{
+    password_ = password;
+}
+
+void WifiConfigurationAp::SetPassword(const std::string &password)
+{
+    password_ = password;
+}
+
 void WifiConfigurationAp::SetSsidPrefix(const std::string &&ssid_prefix)
 {
     ssid_prefix_ = ssid_prefix;
@@ -157,7 +167,18 @@ void WifiConfigurationAp::StartAccessPoint()
     strcpy((char *)wifi_config.ap.ssid, ssid.c_str());
     wifi_config.ap.ssid_len = ssid.length();
     wifi_config.ap.max_connection = 4;
-    wifi_config.ap.authmode = WIFI_AUTH_OPEN;
+    if (password_.empty()) {
+        wifi_config.ap.authmode = WIFI_AUTH_OPEN;
+    } else {
+        if (password_.length() < 8 || password_.length() > 63) {
+            ESP_LOGE(TAG, "AP password must contain 8 to 63 characters");
+            return;
+        }
+        strlcpy((char *)wifi_config.ap.password, password_.c_str(), sizeof(wifi_config.ap.password));
+        wifi_config.ap.authmode = WIFI_AUTH_WPA2_WPA3_PSK;
+        wifi_config.ap.pmf_cfg.capable = true;
+        wifi_config.ap.pmf_cfg.required = false;
+    }
 
     // Start the WiFi Access Point
     ESP_ERROR_CHECK(esp_wifi_set_mode(WIFI_MODE_APSTA));
diff --git a/wifi_manager.cc b/wifi_manager.cc
--- a/wifi_manager.cc
+++ b/wifi_manager.cc
@@ -246,6 +246,7 @@ void WifiManager::StartConfigAp() {
 
     config_ap_->SetSsidPrefix(config_.ssid_prefix);
     config_ap_->SetLanguage(config_.language);
+    config_ap_->SetPassword(config_.ap_password);
 
     // Web handler calls this when user submits config
     config_ap_->OnExitRequested([this]() {
@@ -292,6 +293,11 @@ std::string WifiManager::GetApWebUrl() const {
     return config_ap_->GetWebServerUrl();
 }
 
+std::string WifiManager::GetApPassword() const {
+    std::lock_guard<std::mutex> lock(mutex_);
+    return config_.ap_password;
+}
+
 // ==================== Power ====================
 
 void WifiManager::SetPowerSaveLevel(WifiPowerSaveLevel level) {
